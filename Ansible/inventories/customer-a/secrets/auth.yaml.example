# Copy this file to auth.yaml and fill in the real secret.
# scripts/run-ansible.sh automatically loads auth.yaml when it exists.

ansible_become_password: "change-me"

# Optional: use this for the first SSH key bootstrap run when password login is
# still required. After ssh-copy-id succeeds, you can remove this value.
ansible_password: "change-me"
