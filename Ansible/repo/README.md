# Offline package repositories

Place downloaded Ubuntu 24.04 `.deb` packages here before building an offline
bundle.

```text
repo/prerequisite/
repo/docker/
```

Keep package files matched to the target OS release and architecture.
