# update

Place fixed Ubuntu package `.deb` files here for the `package_update` role.

The offline bundle builder can populate this directory from
`ANSIBLE_PACKAGE_UPDATE_OFFLINE_PACKAGES` in `env.d/90-offline-bundle.env`.
