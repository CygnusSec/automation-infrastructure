# prerequisite

Ubuntu baseline role.

It validates Ubuntu, installs common packages, applies sysctl values, writes system limits, disables swap by default and can optionally disable UFW or set timezone.
